Si necesitas todas las soluciones solo quita el return del primer if dentro del while, y establece que cuando se ejecute el default del switch en vez de ser 2 visitados vuelva  a 0.
Nota: Se le pasan como parametros dos valores enteros, las posiciones a iniciar.
